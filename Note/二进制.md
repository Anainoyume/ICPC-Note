1. 如果一个数字的第 i 位是 1 (从 i = 0 开始), 那么要满足 $a_i~\mathrm{mod}~2^{i+1} \geq 2^i$
当然用 $(1~<<~i)~\&~a_i$ 也是可以的, 不过上面的做法有一种好处:
如果我们判定的时候是 $(c-a_i)~\mathrm{mod}~2^{i+1} \geq 2^i$, 如果 c 是常数，我们需要大量记录位数为 1 的情况的话，这个时候我们可以抽离 $(-a_i)~\mathrm{mod}~2^{i+1}$ 出来用一些数据结构维护。（来源自: 牛客暑假多校2024）