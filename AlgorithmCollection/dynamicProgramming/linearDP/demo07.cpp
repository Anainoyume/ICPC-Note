#include <bits/stdc++.h>
using namespace std;

/* 
    A可以选择i个字串, 选择前j个字符串的字串 拼成B串的前k个字符 的方案数
    dp[i][j][k] 

    dp[i][j][k] = dp[i-1][]
 */

int main(int argc, char const *argv[])
{
    int lenA{}, lenB{}, n{};
    cin >> lenA >> lenB;
    string A{}, B{};
    cin >> A >> B;


    
    system("pause");
    return 0;
}
